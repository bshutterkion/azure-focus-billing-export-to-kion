# Shared defaults for every tenant. Copy to .env and fill in.
#
# WARNING: the Makefile loads this file with `-include .env`, so `make`
# itself parses these values, not just bash: a `#` starts a make comment
# (truncating anything after it, even inside a value) and a bare `$` begins a
# make variable expansion. If a secret below (e.g. KION_API_KEY) legitimately
# contains either character, avoid `#` entirely and write a literal `$` as
# `$$`, or the value will be silently truncated or mangled and the failure
# will only surface later as an authentication error against Kion.

# Kion connection. One Kion serves every tenant.
KION_HOST=
KION_API_KEY=
KION_API_BASE=/api

# Defaults, overridable per tenant. Gov and Commercial tenants are expected to
# be onboarded from the same checkout, so both of these are per-tenant settings
# with a default here. AzureUSGovernment because that's the cloud the tenants
# this tool onboards actually live in; override AZURE_CLOUD per tenant for a
# Commercial one.
AZURE_CLOUD=AzureUSGovernment
BILLING_MODEL=MCA

# FOCUS export settings. FocusCost only: the FOCUS dataset already carries both
# actual (BilledCost) and amortized (EffectiveCost) costs, and Kion cannot read
# the ActualCost/AmortizedCost dataset types.
FOCUS_VERSION=1.0
EXPORT_RECURRENCE=Daily
EXPORT_TIMEFRAME=MonthToDate
EXPORT_PREFIX=focus

# EXPORT_SCOPE default. Kion keeps only the single newest manifest under a
# billing source's prefix, so a tenant with more than one subscription would,
# under `subscription` scope, produce one export/manifest per subscription and
# Kion would silently ingest only one, dropping every other subscription's
# costs with no error. `billingAccount` scope creates a single export (and
# manifest) covering every subscription in the tenant, so it's the safe
# default. `billingAccount`/`billingProfile` scope also requires
# BILLING_SCOPE_ID, which is per-tenant (see tenants/example.env.example).
#
# `subscription` scope is REFUSED under MCA. Verified 2026-09-15 against a real
# MCA billing account with the billing profile's "View charges" policy enabled:
# the export is created, schedules and runs, and writes no rows and no manifest
# at all. Every check this tool performs still passes, so Kion ends up pointed
# at a prefix that never gains a manifest and the tenant shows no spend with no
# error anywhere.
#
# Billing scopes need an MCA or EA billing account. Azure does not offer them
# under a Microsoft Partner Agreement, so a CSP customer tenant can only use
# `subscription` scope -- and therefore can only be onboarded here if it has
# exactly one subscription. That path is untested against a real CSP tenant.
EXPORT_SCOPE=billingAccount

# Default onboarding mode for every tenant. `full` runs storage, exports, the
# app registration and the Kion billing source. `management` runs the app
# registration alone, for tenants whose spend arrives through another tenant's
# export -- which, under a shared MCA billing account, is every customer
# tenant. Override per tenant in tenants/<name>.env.
ONBOARD_MODE=full

# api-version for the Cost Management Exports REST call. Azure moves this
# version over time, and 2023-08-01 and earlier cannot create FOCUS exports at
# all (their ExportType enum has no FocusCost) — only override this if a newer
# default in create-focus-exports.sh ever needs to be pinned back down.
EXPORT_API_VERSION=2025-03-01
